# TOMONOWA Event Portal

GitHub Pages用の静的LPです。OpenAI、DB、サーバー、APIキーは不要です。

## GitHub Pagesで公開する手順

1. GitHubで新規リポジトリ `tomonowa-event-portal` を作成する。
2. このフォルダ内のファイルをすべてアップロードする。
3. GitHubの `Settings` → `Pages` を開く。
4. `Build and deployment` の `Source` を `Deploy from a branch` にする。
5. Branch は `main`、Folder は `/ (root)` を選び、`Save` を押す。
6. 数分後に `https://GitHubユーザー名.github.io/tomonowa-event-portal/` が公開URLになる。

独自ドメインは、GitHub Pagesの `Custom domain` にドメインを入力して、取得先のDNS設定を行えば接続できます。

## 編集場所

- 文章・日付・料金：`index.html`
- 色・余白・スマホ表示：`renewal.css`（旧CSSは読み込んでいません）
- 開催終了・次回開催の表示切替：`site.js`
- 写真・ロゴ：`assets/`

## 2026-09-13 リニューアル

深緑・クリーム・琥珀色の写真を軸にしたイベントサイトへ刷新。トップの実会場写真とロゴは元ファイルを変更せず使用しています。生成画像はコンセプトイメージとして明記し、実会場の写真と区別しています。生成プロンプトは `IMAGE-PROMPTS.md` に保存。

- 日程・料金はチケット風カードに整理。9/11は終了イベントへ移動。
- 写真付きコンセプト紹介、初参加の案内、参加の流れ、実会場・Googleマップ導線を追加。
- スマホ固定申込み導線、FAQ、キーボードフォーカス、動きを抑える設定へ対応。
- JSは閲覧時の日本標準時で開催終了を判定。終了後は該当申込みリンクを除去し、次回日程を更新。全日程終了後は案内待ち表示。新日程はHTMLに追加が必要です。JS無効時はHTMLの初期表示となります。
- 既存の9/18・9/25の申込みURLと料金、FAQの運用ルールを保持。

### 検証

PC、768px、390px、320pxでレイアウトと横はみ出しを確認。全画像の読込み、ページ内リンク、FAQのEnterキー開閉、両日程のGoogleフォームの表示を確認。日時ロジックは9/13、9/18終了1秒前、9/18終了時刻（UTC表現）、9/25終了時刻の4ケースをNode VMで検証。元の会場写真・ロゴにGit差分なし。生成WebPは合計179,126 bytes。

### 既存フォームで見つかった不一致

公開フォームは9/18に「40歳以下・60名限定」、9/25に「33歳以下・60名限定」と記載があります。サイトの過去コミットでは年齢表示が削除されていたため、今回のサイトにも年齢条件は追加していません。フォームの運用条件は変更していません。両フォームにはメールアドレス項目の重複もあります。
